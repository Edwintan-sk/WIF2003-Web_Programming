how to set up
cd backend

npm install

Duplicate the .env.example file inside the backend/ folder and rename the copy to exactly .env.

Replace <PASSWORD> inside their new .env file with the database password

run the server by npm run dev